version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          record: true
          spec: 'cypress/integration/devfest/**'
          start: 'npm start'
          wait-on: 'http://localhost:4100'
